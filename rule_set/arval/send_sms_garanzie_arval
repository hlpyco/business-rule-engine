rule "send_sms_arval_garanzia"
when
    AND(business_partner = "arval",
    event = "call_assistance"
    )
then
    AND(
    print("Sending sms for arval for garanzie"),
    set_variable("var", "value")
    )
end